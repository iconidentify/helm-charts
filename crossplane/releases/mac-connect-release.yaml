apiVersion: helm.crossplane.io/v1beta1
kind: Release
metadata:
  name: mac-connect
  annotations:
    crossplane.io/external-name: mac-connect
spec:
  providerConfigRef:
    name: default
  forProvider:
    chart:
      repository: oci://ghcr.io/iconidentify/charts
      name: mac-connect
      version: "0.2.0"
      pullSecretRef:
        name: ghcr-helm-credentials
        namespace: crossplane-system
    namespace: dialtone-apps
    wait: true
    waitTimeout: 5m0s
  deletionPolicy: Orphan
